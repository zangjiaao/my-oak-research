{
  "name": "gather",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "dotenvx run -- npm run build:scripts",
    "build:scripts": "dotenvx run -- npx tsc -p scripts/tsconfig.json",
    "dev": "dotenvx run -- uv run main.py",
    "start": "dotenvx run -- uv run main.py"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.57.2"
  }
}
