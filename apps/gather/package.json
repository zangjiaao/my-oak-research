{
  "name": "gather",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:scripts": "npx tsc -p scripts/tsconfig.json",
    "dev": "uv run main.py",
    "start": "uv run main.py"
  }
}
