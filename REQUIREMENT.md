## 需求：目前使用的是全文发送，后面可以考虑换成Agent自主判断

在目前的实现中，是每一次请求都插入全文（只要 reportId 存在）。

这种做法属于“全上下文注入”模式。对此，我们需要从以下两个维度来看：

1. 为什么目前选择“全文插入”？
因为这是一个生成式任务（Report Writer），LLM 的核心工作就是围绕这篇报告。绝大多数情况下，无论用户是说“改一下第一段”还是“总结一下全文”，LLM 都需要看到当前的完整状态才能给出准确的修改建议或保持上下文连贯。

优点：响应速度快（不需要额外的 Tool Call 回合），修改精准，不容易产生幻觉（因为原文就在眼前）。
缺点：如果报告非常长（比如几万字），会消耗较多 Token。
2. 什么是你提到的“按需查看”（Agent 模式）？
你提到的“按需查看”通常是指 Tool Use (Function Calling) 模式。在这种模式下：

开始不给 LLM 报告内容。
告诉 LLM 它有一个工具叫 read_current_report。
当用户说“改一下标题”时，LLM 发现自己不知道当前标题，于是通过工具调用来读取报告内容，然后再回答。
目前的差距
目前的 llmGateway.json 只是一个简单的结构化预测（Structured Output），它还不是一个具备自主决策能力的 Agent。

如果你希望把它改成“按需查看”的 Agent，我们需要做以下重构：

引入 AI SDK 的 tool 定义：将 prisma.report.findUnique 封装成一个可被 LLM 调用的工具。
改变交互逻辑：从 generateObject 切换到支持多轮对话和工具调用的模式（如 generateText + tools）。
我的建议
如果报告长度通常在 1-2 万字以内：目前的“全文插入”是性价比最高的。它能保证 LLM 始终掌握最新进度，且逻辑最简单，不容易出错。
如果你的报告是“超长文档”：那么确实需要改成“按需查看”或“分段查看”模式。
你想尝试把这个接口重构成支持 Tool Calling 的 Agent 模式吗？ 这样我们可以给它更多的能力，比如 read_materials（按需读取素材）、search_knowledge（按需搜索知识库）等。